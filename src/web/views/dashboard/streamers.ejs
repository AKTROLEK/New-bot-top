<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>إدارة الستريمرز | Streamer Management</title>
    <link rel="stylesheet" href="/css/dashboard.css">
</head>
<body>
    <%- include('../partials/dashboard-sidebar') %>
    
    <main class="dashboard-main">
        <%- include('../partials/dashboard-header') %>
        
        <div class="dashboard-content">
            <h1>🎬 إدارة الستريمرز</h1>
            
            <div class="stats-row">
                <div class="stat-box glass-effect">
                    <h3>ستريمرز نشطون</h3>
                    <p class="stat-number"><%= streamers.filter(s => s.status === 'approved').length %></p>
                </div>
                <div class="stat-box glass-effect">
                    <h3>طلبات معلقة</h3>
                    <p class="stat-number"><%= streamers.filter(s => s.status === 'pending').length %></p>
                </div>
                <div class="stat-box glass-effect">
                    <h3>إجمالي الستريمرز</h3>
                    <p class="stat-number"><%= streamers.length %></p>
                </div>
            </div>

            <div class="table-container glass-effect">
                <h2>قائمة الستريمرز</h2>
                <table class="data-table">
                    <thead>
                        <tr>
                            <th>المستخدم</th>
                            <th>المنصة</th>
                            <th>الفيديوهات</th>
                            <th>ساعات البث</th>
                            <th>التقييم</th>
                            <th>الحالة</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% streamers.forEach(streamer => { %>
                        <tr>
                            <td><%= streamer.user_id %></td>
                            <td><%= streamer.platform %></td>
                            <td><%= streamer.total_videos %></td>
                            <td><%= (streamer.total_hours || 0).toFixed(1) %> ساعة</td>
                            <td>⭐ <%= (streamer.performance_rating || 0).toFixed(1) %>/10</td>
                            <td>
                                <span class="status-badge <%= streamer.status %>">
                                    <%= streamer.status === 'approved' ? '✅ مقبول' : 
                                        streamer.status === 'pending' ? '⏳ معلق' : '❌ مرفوض' %>
                                </span>
                            </td>
                        </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>
    </main>

    <script src="/js/dashboard.js"></script>
</body>
</html>
